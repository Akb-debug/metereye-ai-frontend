// 🔄 MODIFIÉ — bluetooth.service.ts — service conservé pour compatibilité mais remplacé par DeviceService

import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class BluetoothService {}
